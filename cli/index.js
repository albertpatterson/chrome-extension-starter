#!/usr/bin/env node

import chalk from 'chalk';
console.log(
  chalk.bgBlue(
    'CLI Placholder for cloning templates @ https://github.com/albertpatterson/chrome-extension-starter full CLI in progress'
  )
);
